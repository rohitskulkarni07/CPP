#include <iostream>
#include <string>
#include <vector>
#include <cctype>

using std::cout;
using std::cin;
using std::endl;
using std::vector;
using std::string;

int main(void ) {
	
	vector<int> vi(10,0);

	vector<int>::iterator mid = (vi.begin() + vi.end())/2;
	return (0);
}
